config:
  target: "http://localhost:3003"
  phases:
    - duration: 60
      arrivalRate: 200
  processor: "./fakeIds.js"
scenarios:
  - flow:
    - function: "generateRandomId"
    - get:
        url: "/listings/{{ id }}/similar_listings"
    - log: "Sent a request to id with {{ id }}"
